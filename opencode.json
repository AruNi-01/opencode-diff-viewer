{
  "$schema": "https://opencode.ai/config.json",
  "command": {
    "diff": {
      "template": "Open the lumen diff viewer to show visual git diffs for modified files. $ARGUMENTS",
      "description": "View diff of modified files using lumen TUI"
    }
  }
}
